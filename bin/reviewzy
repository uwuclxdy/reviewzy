#!/usr/bin/env bun
import { runShim } from "../src/shim/main.ts";

runShim().catch((error: unknown) => {
  console.error(error instanceof Error ? error.message : String(error));
  process.exit(1);
});
